<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jikexueyuancrm.mapper.ActivityMapper" >
  <!-- 查询最小推荐日期  -->
  <select id="findActivity" resultType="com.jikexueyuancrm.entity.ActivityBean">
	<![CDATA[SELECT * FROM a_prize_activity WHERE end_time >= NOW() AND start_time <=NOW() AND status = #{status}   ]]>
  </select>
  <!-- &gt; &lt; -->
  
  <select id="findBaseInfo" resultType=""></select>
</mapper>